---
description: Interview until the requirements are settled, then write them up as a spec
---

Define: $ARGUMENTS

1. If the requirements are still open, call the skill tool twice, for "grilling" and "domain-modeling", and interview
   until the user confirms you share an understanding. If this conversation already settled them, go to step 2.
2. Call the skill tool with "to-spec" and write the spec.

When the user has approved the spec, tell them to run `/plan`.
